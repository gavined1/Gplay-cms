<nav class="navbar navbar-expand-lg d-block d-sm-none​​ sticky-top nav-mobile" id="nav-tab" role="tablist">
  <div
    class="container-fluid">
    <!-- Logo on the left -->
    <a class="navbar-brand" href="/">
      <img src="{{ logo.path }}" alt="{{ logo.title }}" height="30" class="d-inline-block align-top"/>
    </a>
    <!-- Menu icon on the right -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="offcanvas"
      data-bs-target="#offcanvasNavbar"
      aria-controls="offcanvasNavbar">
      <span class="navbar-toggler-icon">
        <i class="fa fa-navicon" style="color: #fff; font-size: 28px"></i>
      </span>
    </button>
    <!-- Offcanvas menu -->
    <div class="offcanvas offcanvas-end w-75" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
      <div class="offcanvas-header">
        <button type="button" class="btn-close btn-close-white text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
        <a class="navbar-brand" href="/">
          <img src="{{ logo.path }}" alt="{{ logo.title }}" height="30" class="d-inline-block align-top"/>
        </a>
      </div>
      <div class="offcanvas-body">
        <div class="menu-offcan d-block">
          <div
            class="menu-item d-flex"
            data-bs-toggle="collapse"
            href="#collapseAbout"
            role="button"
            aria-expanded="false"
            aria-controls="collapseAbout">
            <i class="fa fa-circle mx-2"></i>
            <div>អំពី {{ web.name }}</div>
          </div>
        </div>
        <div class="collapse mt-2" id="collapseAbout">
          <div class="card card-body" id="card-body-about">
            <div class="richtext">
              {{ about.title | safe }}
            </div>
          </div>
        </div>
        <hr style="color: #fff;"/>
        <div
          class="menu-item d-flex"
          ​​​
          data-bs-toggle="collapse"
          href="#collapseSocial"
          role="button"
          aria-expanded="false"
          aria-controls="collapseSocial">
          <i class="fa fa-circle mx-2"></i>
          <div>ប្រព័ន្ទផ្សព្វផ្សាយ</div>
        </div>
        <div class="collapse mt-2" id="collapseSocial">
          <div class="card card-body p-2​​" id="card-body-social">
            <div class="row row-cols-5 gap-2 d-flex justify-content-center">
              {% if social_media_links.social_media_links %}
                {% for item in social_media_links.social_media_links %}
                  <div class="col d-flex justify-content-center">
                    <a href="{{ item.link }}" target="_blank">
                      <img src="{{ item.icon }}" alt="{{ item.platform }}" style="height: 30px" class="rounded-3"></a>
                    </div>
                  {% endfor %}
                {% endif %}
              </div>
            </div>
          </div>
          <hr style="color: #fff;"/>
          <div
            class="menu-item d-flex"
            ​​​
            data-bs-toggle="collapse"
            href="#collapseBanks"
            role="button"
            aria-expanded="false"
            aria-controls="collapseBanks">
            <i class="fa fa-circle mx-2"></i>
            <div>ដៃគូរសហការផ្ទេរប្រាក់</div>
          </div>
          <div class="collapse mt-2" id="collapseBanks">
            <div class="card card-body​​" id="card-body-banks">
              <div class="richtext p-2">
                {{ banks.title | safe }}
              </div>
            </div>
          </div>
          <hr style="color: #fff;"/>
          <div
            class="menu-item d-flex"
            ​​​
            data-bs-toggle="collapse"
            href="#collapsePartner"
            role="button"
            aria-expanded="false"
            aria-controls="collapsePartner">
            <i class="fa fa-circle mx-2"></i>
            <div>ដៃគូរសហការវេបសាយ</div>
          </div>
          <div class="collapse mt-2" id="collapsePartner">
            <div class="card card-body p-2 card-body-partner​​">
              <div class="container d-block gap-2 text-white" id="gameProvider">
                {% if partner.partner %}
                  {% for item in partner.partner %}
                    <img src="{{ item.path }}" alt="{{ item.title }}" style="width: 50px">
                    {% endfor %}
                  {% endif %}
                </div>
              </div>
            </div>
            <hr style="color: #fff;"/>
            <div class="container-fluid">
              <div class="d-flex justify-content-center align-items-center ">
                <p style="color: gray; font-size: 11px">
                  Copy right {{ web.name }} @2020. All rights reserved.
                </p>
              </div>
            </div>
          </div>
        </div>
        <!--end offcanvas body-->
      </div>
    </div>
  </nav>